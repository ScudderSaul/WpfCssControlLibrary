            <Window x:Class="WpfCssControlLibrary.Dialogs.ChooseStyleGroup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Css_Classes2019"
            
             mc:Ignorable="d" Title="Choose Css3 classes for Export" MaxWidth="700" MaxHeight="700" FontSize="11"
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer >
        <Border BorderBrush="LightBlue" BorderThickness="2">
            <StackPanel>
                <Button HorizontalAlignment="Right" Margin="4" BorderBrush="Blue" BorderThickness="1" x:Name="CloseButton"
                    Content="Close" Click="CloseButton_OnClick"></Button>
                <WrapPanel x:Name="ChooseWrapPanel">

                </WrapPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Window>

